A *matching* in a graph is a subset of the edges such that no two elements share an endpoint. The *matching number* $\text{ind}_E(G)$ of $G$ is the cardinality of the largest matching in $G$. If there exists a matching using all vertices of $G$, it is called a *perfect matching*

