1. Let $p$ be a prime. Let $G$ be a group of order $p$. Show that $G$ is cyclic. 

>[!note]- Solution
>rahhh

2. Let $G$ be a group. Let $g, h \in G$ and suppose they have orders $m, n$. Suppose $m, n$ are coprime.
	i) Suppose $g^r = h^s$ for some $r, s \in \mathbb{Z}$. Show that $m \mid r$, $n \mid s$ and so $g^r = h^s = 1_G$.
	ii) Show that $g^ah^b = g^ch^d$ if and only if $a \equiv c \mod m$ and $b \equiv d \mod n$
	iii) If $G$ is abelian, show that $gh$ has order $mn$
	iv) Give an example to show that (iii) can be false if $G$ is non-abelian.

>[!note]- Solution
>rahhh

3. For this exercise you may assume Cauchy's Theorem (Let $G$ be a finite group, and let $p$ be a prime dividing $\#G$. Then $G$ has an element of order $p$)
	i) Let $G$ be a finite abelian group with squarefree order. Show that $G$ is cyclic.
	ii) Give an example to show that (i) may be false if $G$ is not assumed to be abelian

>[!note]- Solution
>rahhh

4. Let $H = \{(x, y) \in \mathbb{Z}^2: x + y \equiv 0 \mod 3\}$ Show that $H$ is a subgroup of $\mathbb{Z}^2$. Consider the following cosets of $H$ in $\mathbb{Z}^2$:
	$$(1, 1) + H, \quad (1, 2) + H, \quad (2, 1) + H$$
	i) Show that two of these cosets are equal
	ii) Show that two of these cosets are distinct
	iii) Write down, without proof, a third distinct coset.

>[!note]- Solution
>rahhh

5. For $x, y z \in \mathbb{R}$, check that 
$$\left(\begin{array}{ccc}
1 & x & z \\
0 & 1 & y \\
0 & 0 & 1\\
\end{array}\right)^{-1} = \left(\begin{array}{ccc}
1 & -x& xy - z\\
0 & 1 & -y \\
0 & 0 & 1
\end{array}\right)
$$
Let $$G = \left\{ \left( 
\begin{array}{ccc}
1 & x & z \\ 
0 & 1 & y \\ 
0 & 0 & 1\\ 
\end{array} \right) 
: x, y , z\in \mathbb{R} \right\}
$$ It's easy to check that $G$ is a subgroup of $GL_3(\mathbb{R})$ and $H$ is a subgroup of $G$.
	i) is $G$ normal in $GL_3(\mathbb{R})$?
	ii) Is $H$ normal in $G$? 

>[!note]- Solution
>rahhh

